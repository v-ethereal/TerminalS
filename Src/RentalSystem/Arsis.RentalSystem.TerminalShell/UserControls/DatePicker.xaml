<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:UserControls="clr-namespace:Arsis.RentalSystem.TerminalShell.UserControls">

    <SolidColorBrush x:Key="SolidBlack" Color="Black"/>
    <SolidColorBrush x:Key="SolidSilver" Color="Silver"/>

    <LinearGradientBrush x:Key="NormalBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#FFF" Offset="0.0"/>
                <GradientStop Color="#CCC" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="DarkBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#FFF" Offset="0.0"/>
                <GradientStop Color="#AAA" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PressedBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#BBB" Offset="0.0"/>
                <GradientStop Color="#EEE" Offset="0.1"/>
                <GradientStop Color="#EEE" Offset="0.9"/>
                <GradientStop Color="#FFF" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="DisabledForegroundBrush" Color="#888" />

    <SolidColorBrush x:Key="DisabledBackgroundBrush" Color="#EEE" />

    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFF" />

    <SolidColorBrush x:Key="SelectedBackgroundBrush" Color="#DDD" />

    <!-- Border Brushes -->

    <LinearGradientBrush x:Key="NormalBorderBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#CCC" Offset="0.0"/>
                <GradientStop Color="#444" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="HorizontalNormalBorderBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#CCC" Offset="0.0"/>
                <GradientStop Color="#444" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="PressedBorderBrush" StartPoint="0,0" EndPoint="0,1">
        <GradientBrush.GradientStops>
            <GradientStopCollection>
                <GradientStop Color="#444" Offset="0.0"/>
                <GradientStop Color="#888" Offset="1.0"/>
            </GradientStopCollection>
        </GradientBrush.GradientStops>
    </LinearGradientBrush>

    <SolidColorBrush x:Key="DisabledBorderBrush" Color="#AAA" />

    <SolidColorBrush x:Key="SolidBorderBrush" Color="#888" />

    <SolidColorBrush x:Key="LightBorderBrush" Color="#AAA" />

    <!-- Miscellaneous Brushes -->
    <SolidColorBrush x:Key="GlyphBrush" Color="#444" />

    <SolidColorBrush x:Key="LightColorBrush" Color="#DDD" />

    <!--Converters section-->
    <UserControls:CellSizeConverter x:Key="CellSizeConverter"/>
    <UserControls:MonthConverter x:Key="MonthConverter"/>

    <!--DatePicker Template-->

    <Style x:Key="CalendarListBoxStyle" TargetType="{x:Type ListBox}">
        <Setter Property="ItemsPanel">
            <Setter.Value>
                <ItemsPanelTemplate>
                    <WrapPanel Width="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ListBox}}, Path=ActualWidth, Converter={StaticResource CellSizeConverter}, ConverterParameter=widthCellContainer }" />
                </ItemsPanelTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ListBoxItem">
                    <Setter Property="IsEnabled" Value="{Binding IsEnabled}"/>
                    <Setter Property="Width" Value="90"/>
                    <Setter Property="VerticalContentAlignment" Value="Center" />
                    <Setter Property="HorizontalContentAlignment" Value="Center" />
                    <Setter Property="FontSize" Value="36" />
                    <Setter Property="MinHeight" Value="90"/>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>

    <!--DateTimePicker default style-->
    <Style TargetType="{x:Type UserControls:DatePicker}">

        <!--DataTemplate for the DayCell-->
        <Setter Property="DayCellTemplate">
            <Setter.Value>
                <DataTemplate>
                    <TextBlock Text="{Binding Path=DayNumber}" x:Name="dayNumberCellText" />
                                        <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding IsInCurrentMonth}" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource SolidSilver}" TargetName="dayNumberCellText"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </Setter.Value>
        </Setter>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type UserControls:DatePicker}">
                    <StackPanel>
                        <Grid Width="{TemplateBinding Width}" Name="containerGrid" MinHeight="170"
                                  Background="{TemplateBinding Background}">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="85"/>
                                <RowDefinition Height="85"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <DockPanel LastChildFill="True">
                                <Button Name="PART_MonthBack" Grid.Column="0" 
                                            Style="{TemplateBinding MonthBackButtonStyle}"
                                            HorizontalAlignment="Left"
                                            DockPanel.Dock="Left"/>

                                <Button Name="PART_MonthForward" Grid.Column="2"
                                            Style="{TemplateBinding MonthForwardButtonStyle}"
                                            HorizontalAlignment="Right"
                                            DockPanel.Dock="Right"/>

                                <WrapPanel Grid.Column="1" HorizontalAlignment="Center">
                                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center" Text="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControls:DatePicker}}, Path=CurrentlyViewedMonth, Mode=TwoWay, Converter={StaticResource MonthConverter}}"                                              
                                              Style="{TemplateBinding MonthSelectorStyle}" FontSize="36"/>

                                    <TextBlock Margin="2,0,0,0" FontSize="36" HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center">
                                        <TextBlock.Text>
                                            <Binding Path="CurrentlyViewedYear" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged" 
                                                     RelativeSource="{RelativeSource FindAncestor, AncestorType={x:Type UserControls:DatePicker}}">
                                                <Binding.ValidationRules>
                                                    <UserControls:YearValidation />
                                                </Binding.ValidationRules>
                                            </Binding>
                                        </TextBlock.Text>
                                    </TextBlock>
                                </WrapPanel>
                            </DockPanel>

                            <Grid Grid.Row="1">
                                <Grid.Resources>
                                    <!--Style to apply the DayHeaderTemplate to all ContentControls-->
                                    <Style TargetType="ContentControl">
                                        <Setter Property="ContentTemplate" Value="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=DayHeaderTemplate}"/>
                                    </Style>
                                </Grid.Resources>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <ContentControl Content="Пн" Grid.Column="0"/>
                                <ContentControl Content="Вт" Grid.Column="1"/>
                                <ContentControl Content="Ср" Grid.Column="2"/>
                                <ContentControl Content="Чт" Grid.Column="3"/>
                                <ContentControl Content="Пт" Grid.Column="4"/>
                                <ContentControl Content="Сб" Grid.Column="5"/>
                                <ContentControl Content="Вс" Grid.Column="6"/>
                            </Grid>
                            <ListBox Grid.Row="2" Name="PART_Dates" Style="{StaticResource CalendarListBoxStyle}"  />
                        </Grid>
                    </StackPanel>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>