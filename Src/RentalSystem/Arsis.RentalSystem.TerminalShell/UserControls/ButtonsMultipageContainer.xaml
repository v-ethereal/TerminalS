<UserControl x:Class="Arsis.RentalSystem.TerminalShell.UserControls.ButtonsMultipageContainer"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="232" Width="728">
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="100"/>
        </Grid.RowDefinitions>
        <WrapPanel Grid.Row="0" Name="buttonsContainer" Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" />
        <Button Grid.Row="1" Height="70" Style="{DynamicResource KeyboardButtonStyle}" HorizontalAlignment="Left" Name="btPrev" VerticalAlignment="Center" Width="70" Click="btPrevious_Click" Margin="10,0,0,20">&lt;&lt;
            <Button.BitmapEffect>
                <OuterGlowBitmapEffect Noise="0" Opacity="0.5" GlowSize="20" GlowColor="Red" />
            </Button.BitmapEffect>
        </Button>
        <Button Grid.Row="1" Height="70" Style="{DynamicResource KeyboardButtonStyle}" HorizontalAlignment="Right" Name="btNext" VerticalAlignment="Center" Width="70" Click="btNext_Click" Margin="0,0,10,20">
            <Button.BitmapEffect>
                <OuterGlowBitmapEffect GlowSize="20" Opacity="0.5" GlowColor="Red" />
            </Button.BitmapEffect> &gt;&gt;</Button>
    </Grid>
</UserControl>
